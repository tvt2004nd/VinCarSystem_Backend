spring.application.name=VinSystem

spring.datasource.url=jdbc:mysql://localhost:3306/vinsystem?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Ho_Chi_Minh
spring.datasource.username=root
spring.datasource.password=${DB_PASSWORD:123456}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.default_batch_fetch_size=10
spring.jpa.properties.hibernate.jdbc.batch_size=10
spring.jpa.properties.hibernate.jdbc.fetch_size=50
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.use_sql_comments=false

server.port=8080
spring.security.user.name=tranvantien
spring.security.user.password=${BASIC_AUTH_PASSWORD:123456}

# mail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:tvt2004nd@gmail.com}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.default-encoding=UTF-8
spring.mail.from=VinCar

logging.level.org.springframework.security=DEBUG

# upload
file.upload-dir=D:/codenew/E-commerce_Back/uploads
file.upload-video=C:/my-application-data/uploads-video
file.upload-lesson-assignment=C:/my-application-data/uploads-lesson-assignment
spring.servlet.multipart.max-file-size=500MB
spring.servlet.multipart.max-request-size=500MB

# Security
jwt.secret=${JWT_SECRET:VinSystemSecretKeyVinSystemSecretKey1234567890}
jwt.expiration=86400000

# Vietmap
vietmap.api.key=${VIETMAP_API_KEY:}

# Chat


server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true

# ── VNPay ────────────────────────────────────────────────────────────────────
vnpay.tmn-code=24NFFNX4
vnpay.hash-secret=${VNPAY_HASH_SECRET:}
vnpay.sandbox-url=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html

# ReturnUrl → trỏ thẳng về frontend
# VNPay redirect browser về đây, frontend đọc params rồi gọi POST /api/vnpay/process-return
vnpay.return-url=http://localhost:3000/payment/result

# Không dùng nữa nhưng giữ để không bị lỗi @Value
vnpay.frontend-result-url=http://localhost:3000/payment/result

gemini.api.key=${GEMINI_API_KEY:}
# Admin local
admin.username=admin
admin.password=admin123
admin.email=admin@vin.com
# Cloudinary
cloudinary.cloud-name=dsgnwwxaa
cloudinary.api-key=998958675859428
cloudinary.api-secret=${CLOUDINARY_API_SECRET:}
sendgrid.api.key=${SENDGRID_API_KEY}

mail.from=tvt2004nd@gmail.com